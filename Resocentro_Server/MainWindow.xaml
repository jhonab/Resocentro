<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" x:Class="Resocentro_Server.MainWindow"
        Title="MainWindow" Height="350" Width="525" Background="{DynamicResource Windows.Background}">
    <Grid>

        <telerik:RadTabControl>
            <telerik:RadTabItem Header="Envio Facturas">
                <Grid Background="{DynamicResource Windows.Background}">
                    <TextBox TextWrapping="Wrap"  IsReadOnly="True" Text="" Foreground="White" Background="{DynamicResource Windows.Background}"/>
                </Grid>
            </telerik:RadTabItem>
        </telerik:RadTabControl>

    </Grid>
</Window>
